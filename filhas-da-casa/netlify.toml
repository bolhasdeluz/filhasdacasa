[build]
  publish   = "public"
  functions = "netlify/functions"
  command   = "echo 'Sem build step — só HTML estático + funções'"

[functions]
  node_bundler = "esbuild"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options         = "SAMEORIGIN"
